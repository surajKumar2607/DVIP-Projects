This will show the morphological operation in image processing,
The code is in Python.
Any kernal can be used to get the eroded and diluted image,
as the size of kernal increases the time to execute also increases.

The code zero pad the original image, based on the kernal size new image is created and execution of the
operation the padded zero is removed. The image is displayed using pyplot in mathplotlib 